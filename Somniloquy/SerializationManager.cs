namespace Somniloquy {
    using System;
    using System.Collections.Generic;
    using System.Text;

    using Microsoft.Xna.Framework;
    using Microsoft.Xna.Framework.Graphics;

    public static class SerializationManager {
        public static string ReadFile(Type type, string fileName) {
            return "";
        }

        public static void WriteFile(Type type, string fileName, string serialized) {
            // TODO: Add functionality to compress serialized string and save/load it.
        }
    }
}